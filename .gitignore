.DS_Store
node_modules/
.env
.env.local

# Agent-runtime mirror directories — leftover from npx skills CLI experiments.
# sync.sh writes only to skills/; these can be removed and stay ignored.
.agents/
.claude/
